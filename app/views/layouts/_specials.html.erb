<div id="specialsbox" class="insets">
	<h1 class="pagetitle">Specials</h1>
	<div id="linkbox">
		<% @specials.each do |special| %>
			<img class="speciallink" src="<%= special[1][:file] %>" data-viewid="<%= special[2][:file] %>" data-resourceid="<%= special[0].resource[:path] %>" alt="link">
		<% end %>
	</div>	
	<div id="printbutton"><a href="<%= @specials.first[2][:file] %>" target="_blank">(printable version)</a></div>
	<div id="contain">
		<img id="specialviewer" alt="special" />
	</div>
</div>

<script>
//"http://docs.google.com/gview?url=http://byswebservices.com:3100/" + $(this).data('resourceid') + "&embedded=true"
	$(".speciallink").click(function(){ 
		console.log(1);
		$("#specialviewer").attr('src', $(this).data('viewid'));
		console.log(2);
		$("#printbutton a").attr("href",$(this).data("resourceid"));
		console.log(3);
	});
	$($(".speciallink")[0]).click();
	/* var Opera = (navigator.userAgent.match(/Opera|OPR\//) ? true : false);
	if (Opera != true) {
	 	
	} */
</script>